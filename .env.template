# Copy this file to `.env` and fill in values (`.env` is gitignored).
# Only required for `pnpm chromatic`. Local `pnpm test:devex` works without a `.env` file.

# Required for `pnpm chromatic`
CHROMATIC_PROJECT_TOKEN=

# QA Hub namespace — fixed for this repro; do not change
AZURE_NAMESPACE=mb-chromatic

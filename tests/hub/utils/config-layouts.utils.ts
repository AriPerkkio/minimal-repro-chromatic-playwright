import { Locator } from '@playwright/test';

/** Stub — only referenced by unused DevexUtils layout-assignment helpers in the copied util file. */
export class ConfigLayoutsUtils {
    public static layoutAssignmentApplicationPlaceGrid: Locator;

    public static async initiateLayoutAssignmentsTabElements(_aPage: unknown): Promise<void> {
        // no-op stub
    }
}

export enum DxColumnChooseDirection {
    FROM_COLUMN_CHOOSER_TO_COLUMN = 'From Column Chooser to Column',
    FROM_COLUMN_TO_COLUMN_CHOOSER = 'From Column to Column Chooser',
}

export enum DxFilterNumber {
    BETWEEN = 'Between',
    EQUALS = 'Equals',
    GREATER_THAN = 'Greater than',
    GREATER_THAN_OR_EQUAL_TO = 'Greater than or equal to',
    LESS_THAN = 'Less than',
    LESS_THAN_OR_EQUAL_TO = 'Less than or equal to',
}

export enum DXFilterSortDirection {
    ASCENDING = 'asc',
    DESCENDING = 'desc',
}

export enum DxFilterText {
    CONTAINS = 'Contains',
    DOES_NOT_CONTAIN = 'Does not contain',
    ENDS_WITH = 'Ends with',
    STARTS_WITH = 'Starts with',
}
